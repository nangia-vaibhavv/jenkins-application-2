pipeline {
    agent any
    stages() {
        stage('Build') {
            echo "building in progress"
        }
        stage("Test"){
            echo "testing in progress"
        }
        stage("deploy") {
            echo "deployment in progress"
        }
    }
}